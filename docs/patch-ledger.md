# Patch Ledger

Use this file to track every fork-specific patch that increases maintenance cost.

| Status | Scope | Files | Reason | Classification | Rebase Cost | Replacement Plan |
|---|---|---|---|---|---|---|
| open | bootstrap | `README.md`, `QHAPAQXIAN.md`, `docs/*`, `.github/workflows/qhapaqxian-bootstrap.yml`, `src/backend/qx/*`, `src/include/qx/*` | establish fork identity, governance, CI, and subsystem roots | `PERM_SUBSYSTEM` | low | none |
| noted | compatibility policy | `src/include/catalog/catversion.h` | no change in bootstrap because no fork-owned catalog divergence exists yet | `TEMP_PATCH` | low | take control of `catversion.h` only when catalog/bootstrap state diverges |
| open | stage-2 contract | `docs/stage-2/*` | freeze public language and parser-readiness contract before Stage 3 edits | `PERM_SUBSYSTEM` | low | superseded later by parser/catalog/runtime implementation docs, not removed |
| open | stage-3 parser boundary | `src/backend/parser/gram.y`, `src/include/parser/kwlist.h`, `src/include/nodes/parsenodes.h`, `src/backend/tcop/utility.c`, `src/include/tcop/cmdtaglist.h`, `src/backend/commands/{agentcmds.c,sessioncmds.c,taskcmds.c}`, `src/include/commands/{agentcmds.h,sessioncmds.h,taskcmds.h}`, `src/test/regress/{parallel_schedule,sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `docs/stage-3/*` | make agentic commands native to parser/AST/utility flow without faking catalogs or runtime | `PERM_SUBSYSTEM` | medium | keep clause order narrow until Stage 4/5 justify broader grammar flexibility |
| open | stage-4 engine catalogs | `src/include/catalog/{pg_qx_agent.h,pg_qx_session.h,pg_qx_task.h,pg_qx_step.h,pg_qx_event.h,pg_qx_trace.h,pg_qx_checkpoint.h}`, `src/include/catalog/{Makefile,meson.build,catversion.h}`, `src/test/regress/expected/oidjoins.out` | persist agent metadata and runtime-owned task state inside engine-managed catalogs | `PERM_SUBSYSTEM` | medium | keep runtime rows on heap catalogs until Stage 10 proves deeper storage is necessary |
| open | stage-5 vertical slice | `src/backend/commands/{sessioncmds.c,taskcmds.c}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `docs/stage-5/*` | execute a single-node task slice with durable events, traces, steps, and checkpoints before introducing scheduler/bgworker complexity | `TEMP_PATCH` | medium | replace synthetic synchronous step materialization with Stage 6 runtime dispatcher and later agent planner/executor paths |
| open | stage-6 embedded runtime | `src/backend/{Makefile,meson.build}`, `src/backend/qx/{Makefile,meson.build,runtime/*}`, `src/include/qx/qx_runtime.h`, `src/backend/commands/taskcmds.c`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `docs/stage-6/*` | move task execution behind a runtime-owned submit/dispatch boundary before introducing bgworkers and scheduler concurrency | `PERM_SUBSYSTEM` | medium | extend this module with launcher, workers, queues, retries, and recovery instead of pushing new runtime logic back into command handlers |
| open | stage-7 resumable runtime | `src/include/catalog/{pg_qx_attempt.h,pg_qx_task.h,pg_qx_checkpoint.h}`, `src/backend/{parser/gram.y,tcop/utility.c,nodes/gen_node_support.pl}`, `src/include/{nodes/parsenodes.h,parser/kwlist.h,tcop/cmdtaglist.h,commands/taskcmds.h,qx/qx_runtime.h}`, `src/backend/{commands/taskcmds.c,qx/runtime/qx_runtime.c}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out,expected/oidjoins.out}`, `docs/stage-7/*` | formalize durable attempts, resumable checkpoints, and native `RESUME TASK` utility execution before asynchronous runtime work | `PERM_SUBSYSTEM` | high | extend into crash requeue/recovery workers, compensation, and deeper agent transaction rules in later stages |
| open | stage-8 planner/executor boundary | `src/backend/qx/{Makefile,meson.build,planner/*,executor/*}`, `src/include/qx/{qx_agent_plan.h,qx_agent_planner.h,qx_agent_executor.h}`, `src/backend/{commands/taskcmds.c,parser/gram.y,tcop/utility.c,nodes/gen_node_support.pl}`, `src/include/{nodes/parsenodes.h,tcop/cmdtaglist.h}`, `src/backend/qx/runtime/qx_runtime.c`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `docs/stage-8/*` | introduce fork-owned agent planning/execution metadata and native `EXPLAIN AGENT` before deeper planner/executor integration | `PERM_SUBSYSTEM` | high | evolve `AgentPlan` into richer costed branching/tool semantics and later bind it to deeper executor infrastructure instead of collapsing back into utility handlers |
| open | stage-9 semantic WAL boundary | `src/backend/qx/{Makefile,meson.build,replication/*}`, `src/include/qx/qx_semantic_log.h`, `src/include/catalog/{pg_qx_event.h,pg_qx_trace.h,pg_qx_checkpoint.h,catversion.h}`, `src/backend/{commands/sessioncmds.c,qx/runtime/qx_runtime.c}`, `contrib/qhapaqxian_output/*`, `contrib/{Makefile,meson.build}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `docs/stage-9/*` | emit transactional semantic logical messages, persist correlated `LSN`s, and expose a fork-owned logical decoder over semantic payloads before deeper replication work | `PERM_SUBSYSTEM` | high | add recovery scanner, downstream subscription control, and dedicated WAL families only if logical-message boundaries prove insufficient |
| open | stage-10 memory storage surface | `src/include/catalog/{pg_qx_memory.h,Makefile,meson.build,catversion.h}`, `src/include/{commands/memorycmds.h,commands/tracecmds.h,qx/qx_memory.h}`, `src/backend/{commands/{memorycmds.c,tracecmds.c},qx/{Makefile,meson.build,memory/*},tcop/utility.c}`, `src/include/{nodes/parsenodes.h,parser/kwlist.h,tcop/cmdtaglist.h}`, `src/backend/parser/gram.y`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out,expected/oidjoins.out}`, `docs/stage-10/*` | persist engine-owned agent memory and expose native operator commands for memory retrieval and trace inspection without committing yet to a fork-specific storage engine | `PERM_SUBSYSTEM` | high | keep `pg_qx_memory` on heap/index storage until benchmark evidence justifies custom AMs, vector structures, or deeper semantic retrieval operators |
| open | stage-11 operability surface | `src/backend/catalog/system_views.sql`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out,expected/rules.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-11/*}` | expose stable operator-facing `pg_stat_qx_*` views and close bootstrap hardening with aligned regression/deparser coverage | `PERM_SUBSYSTEM` | medium | keep telemetry as system views over engine catalogs until scale or privilege pressure justifies dedicated stats collectors or security-barrier reporting paths |
| open | windows branded launcher | `src/bin/{meson.build,Makefile,qhapaqxian_launcher/*}`, `docs/{patch-ledger.md,release-bootstrap.md}` | provide a branded Windows entrypoint `qhapaqxian-db.exe` that forwards to sibling `postgres.exe` without yet renaming upstream binaries or changing server internals | `TEMP_PATCH` | low | replace with first-class product packaging or true binary renaming only when release engineering and compatibility policy are ready |
| open | stage-12 security isolation seed | `src/backend/catalog/system_views.sql`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out,expected/rules.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-12/*}` | hide `pg_stat_qx_*` rows from non-owners by default and revoke direct public reads on raw `pg_qx_*` catalogs while owner-aware command paths remain engine-enforced | `PERM_SUBSYSTEM` | medium | evolve from owner-based visibility toward explicit agent identities, namespaces, policy catalogs, and dedicated security roles instead of relying mainly on role ownership |
| open | stage-13 operational identities | `src/include/catalog/{pg_qx_identity.h,pg_qx_agent.h,pg_qx_session.h,pg_qx_task.h,Makefile,meson.build,catversion.h}`, `src/include/qx/{qx_security.h,qx_agent_plan.h,qx_runtime.h}`, `src/backend/{commands/{agentcmds.c,sessioncmds.c},qx/{Makefile,meson.build,security/*,planner/qx_agent_planner.c,runtime/qx_runtime.c,executor/qx_agent_executor.c}}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out,expected/oidjoins.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-13/*}` | materialize engine-owned identities, snapshot identity/tools/budget into session and task state, and enforce budget ceilings during plan admission before runtime submission | `PERM_SUBSYSTEM` | high | evolve from identity/budget snapshots into explicit namespace policy catalogs, live runtime budget metering, and tool-execution authorization rather than keeping authorization as planner metadata only |
| open | stage-14 namespace policy and runtime authorization | `src/include/catalog/{pg_qx_namespace.h,pg_qx_tool.h,pg_qx_agent.h,pg_qx_session.h,pg_qx_task.h,Makefile,meson.build,catversion.h}`, `src/include/qx/{qx_security.h,qx_agent_plan.h,qx_runtime.h}`, `src/backend/{catalog/system_views.sql,commands/{agentcmds.c,sessioncmds.c},qx/{Makefile,meson.build,security/*,planner/qx_agent_planner.c,runtime/qx_runtime.c,executor/qx_agent_executor.c}}`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/{qx_stage3_agentic.out,oidjoins.out,rules.out}}`, `README.md`, `docs/{bootstrap-plan.md,stage-14/*}` | materialize schema-bound namespace policy, authorize tools before runtime submission, and meter live task consumption inside the embedded runtime rather than stopping at planner-side admission checks | `PERM_SUBSYSTEM` | high | replace namespace-bound bootstrap policy with explicit policy DDL and move from allowlist/accounting scaffolds to executable tool descriptors, runtime isolation, and provider-backed metering |
| open | stage-15 namespace policy DDL and tool registry | `src/backend/{commands/qxpolicycmds.c,parser/gram.y,tcop/utility.c}`, `src/include/{commands/qxpolicycmds.h,nodes/parsenodes.h,parser/kwlist.h,tcop/cmdtaglist.h}`, `src/include/catalog/{pg_qx_namespace.h,pg_qx_tool.h,catversion.h}`, `src/backend/qx/{security/qx_security.c,planner/qx_agent_planner.c,executor/qx_agent_executor.c}`, `src/backend/commands/agentcmds.c`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-15/*}` | make namespace policies and tool descriptors first-class DDL, fix the one-policy-per-schema historical bug, move authorization/metering onto real `pg_qx_tool` rows, and remove implicit namespace-policy creation from `CREATE AGENT` | `PERM_SUBSYSTEM` | high | keep sandbox/principal metadata in engine catalogs for now, then evolve into provider-backed execution, stronger sandboxing, and metering tied to actual tool runtime events |
| open | stage-16 principal runtime and external metering | `src/include/catalog/{pg_qx_principal.h,pg_qx_tool.h,Makefile,meson.build,catversion.h}`, `src/include/{commands/qxpolicycmds.h,nodes/parsenodes.h,parser/kwlist.h,qx/qx_security.h,tcop/cmdtaglist.h}`, `src/backend/{commands/qxpolicycmds.c,parser/gram.y,tcop/utility.c,qx/{runtime/qx_runtime.c,security/qx_security.c}}`, `src/bin/{Makefile,meson.build,qhapaqxian_tool_runner/*}`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/{qx_stage3_agentic.out,oidjoins.out}}`, `README.md`, `docs/{bootstrap-plan.md,stage-16/*}` | materialize namespace principals, launch tool execution out-of-process through shipped runner binaries, enforce sandbox ceilings at runtime, and charge task budgets from external runner responses instead of descriptor-only constants | `PERM_SUBSYSTEM` | high | replace bindir-scoped helper execution with stronger sandbox/container or microVM isolation and eventually swap fork-owned metering for provider-backed execution receipts |
| open | stage-17 stronger runtime sandboxing | `src/backend/qx/runtime/qx_runtime.c`, `src/bin/qhapaqxian_tool_runner/*`, `src/backend/commands/tracecmds.c`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-17/*}` | turn principal sandbox labels into OS-level launcher profiles with minimal environments, sealed runtime workdirs, timeout/process ceilings, and trace-visible enforcement evidence instead of relying only on catalog validation | `PERM_SUBSYSTEM` | high | replace launcher-level process controls with stronger per-principal isolation domains such as alternate OS principals, containers, or microVM-backed execution when runtime providers justify the deeper divergence |
| open | stage-18 restricted identity launch receipts | `src/backend/qx/runtime/qx_runtime.c`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-18/*}` | execute non-`builtin` principals under restricted Windows tokens and record parent-observed launch evidence (`restricted_identity`, `wall_ms`, launch mode) so sandbox traces are not dependent only on child-reported metadata | `PERM_SUBSYSTEM` | high | if the product later adopts brokered/containerized providers, move restricted-identity evidence into provider attestation or remote receipts instead of direct local process launch |
| open | stage-19 provider catalog and receipt verification | `src/include/catalog/{pg_qx_provider.h,pg_qx_principal.h,Makefile,meson.build,catversion.h}`, `src/include/{commands/qxpolicycmds.h,nodes/parsenodes.h,parser/kwlist.h,qx/qx_security.h,tcop/cmdtaglist.h}`, `src/backend/{commands/qxpolicycmds.c,parser/gram.y,tcop/utility.c,qx/{security/qx_security.c,runtime/qx_runtime.c}}`, `src/bin/qhapaqxian_tool_runner/*`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/{qx_stage3_agentic.out,oidjoins.out}}`, `README.md`, `docs/{bootstrap-plan.md,stage-19/*}` | materialize runtime providers as engine-owned catalog objects and require verifiable provider receipts (`provider`, `receipt_schema`, `receipt_nonce`, attestation mode) before external tool execution is accepted and budget-charged | `PERM_SUBSYSTEM` | high | extend `loopback` provider receipts into remote/signed provider attestations or container/microVM-backed execution only when the product actually leaves the single-host provider boundary |
| open | stage-20 brokered remote providers and signed receipts | `src/include/catalog/{pg_qx_provider.h,catversion.h}`, `src/include/nodes/parsenodes.h`, `src/backend/{parser/gram.y,commands/qxpolicycmds.c}`, `src/backend/qx/{security/qx_security.c,runtime/qx_runtime.c,planner/qx_agent_planner.c}`, `src/bin/qhapaqxian_tool_runner/*`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-20/*}` | accept brokered `remote://` providers, add provider-scoped receipt keys, and require HMAC-signed receipts plus provider-kind-specific attestation modes before runtime accepts external execution | `PERM_SUBSYSTEM` | high | replace shared-key HMAC receipts with asymmetric provider signatures or container/microVM attestation only when the product adopts a truly remote or isolated runtime plane |
| open | stage-21 asymmetric remote receipts | `src/include/catalog/{pg_qx_principal.h,pg_qx_provider.h,catversion.h}`, `src/include/{nodes/parsenodes.h,parser/kwlist.h}`, `src/backend/{parser/gram.y,commands/qxpolicycmds.c}`, `src/backend/qx/{security/qx_security.c,runtime/qx_runtime.c,planner/qx_agent_planner.c}`, `src/bin/qhapaqxian_tool_runner/{meson.build,qhapaqxian_tool_runner.c}`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `README.md`, `docs/{bootstrap-plan.md,stage-21/*,testing-bootstrap.md}`, `.github/workflows/qhapaqxian-bootstrap.yml` | replace shared-key remote receipts with provider-verified `ed25519` receipts, bind principals to signer material, and stage OpenSSL runtime DLLs into `bindir` so minimal-environment Windows execution can verify asymmetric receipts without reintroducing `PATH` into the sandbox | `PERM_SUBSYSTEM` | high | keep the brokered runner model for now, then move toward certificate-chain or hardware-rooted attestation and eventually container/microVM-backed principals when the runtime leaves the single-host broker boundary |
| open | stage-22 brokered container and microVM principal classes | `src/include/catalog/{pg_qx_principal.h,pg_qx_provider.h,catversion.h}`, `src/include/{nodes/parsenodes.h,parser/kwlist.h}`, `src/backend/{parser/gram.y,commands/qxpolicycmds.c}`, `src/backend/qx/{security/qx_security.c,runtime/qx_runtime.c,planner/qx_agent_planner.c}`, `src/bin/qhapaqxian_tool_runner/qhapaqxian_tool_runner.c`, `src/test/regress/{sql/qx_stage3_agentic.sql,expected/qx_stage3_agentic.out}`, `contrib/qhapaqxian_output/{sql/semantic_messages.sql,expected/semantic_messages.out}`, `README.md`, `docs/{bootstrap-plan.md,patch-ledger.md,stage-22/*,testing-bootstrap.md}` | introduce explicit principal runtime classes (`host`, `container`, `microvm`), add brokered `container://` and `microvm://` providers, and require receipt/tracing payloads to bind execution evidence to the expected runtime class before the engine accepts external work | `PERM_SUBSYSTEM` | high | keep the brokered single-host runner model for now, then swap provider backends to real container or microVM launchers when the product is ready to own that deeper platform boundary |

Legend:
- `TEMP_PATCH`: short-lived bridge toward a later subsystem.
- `PERM_SUBSYSTEM`: durable fork investment.
- `UPSTREAMABLE_CANDIDATE`: patch that might be proposed upstream later.
